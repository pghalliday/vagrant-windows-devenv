vagrant-windows-devenv
======================

Vagrant development environment to work on the `vagrant-windows` plugin.

Clone next the `vagrant-windows` project and it will be mounted at `/vagrant-windows`.
